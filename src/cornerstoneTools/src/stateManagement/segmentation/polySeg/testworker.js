import { expose } from 'comlink';
const polySegConverters = {
    name: "xionghailong",
    async convertLabelmapToSurface(args, ...callbacks) {
        console.log("testWorker 执行.");
        return name;
    },
};
expose(polySegConverters);
